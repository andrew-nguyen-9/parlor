# e3-proposal — done

shipped: `.orchestrator/proposals/e3-design.md` — E3.1 light mode (full role remap + computed WCAG table), E3.2 guidelines (color-role law, --d-* rhythm, mobile/desktop, one-light-source cursor rule, motion budget), E3.3 refs (lusion.co, poolsuite.net, obys.agency), machine token table for e3-tokens.
files: proposals/e3-design.md only (analysis unit — no code).
decided: extend not rebuild — only 2 light values change (brass, gold); new `--c-focus` token; new two-tier jewels (fill=CATEGORY_HEX unchanged, ink=--cat-* text-safe per theme); deck card faces declared theme-invariant (zero deck churn); all ratios computed, every text pair ≥4.5.
gotchas: found 2 live a11y bugs proposal fixes — focus ring hardcoded #e6c878 is ~1.3:1 (invisible) on light bg; dark jewels music/screen/wildcard fail AA as text today (wildcard 2.84 fails even large). globals.css is §3.0-frozen: edits framed as the sanctioned token follow-up.
branch: none (analysis unit; held for user sign-off A5 before e3-tokens implements).
