# Decisions — big-cycle 2026-07 (landed 2026-07-07, `main` @ 613c1ff)

16 units, orchestrated A→D. Foundation F0–F6 + game revamps G1–G9. Blind-judge panel all PASS.
Harvested from `.orchestrator/*.done.md` before reset. Next cycle's intake (A) reads this.

## Cross-cutting traps (read these first)

- **Serena `replace_in_files` / `replace_content` / `insert_*` silently no-persist.** Reported success but wrote nothing to disk on `queries.ts` (F6), 10 `page.tsx` edits (F3), and others. **Always `git diff` after a Serena write; fall back to the Edit tool.** Hit repeatedly across the cycle.
- **Engine signature is `generate<X>(dayIndex:number, date:string)`** — `string`, NOT `Date` (the F6 brief/contract said `Date`, but the seance template + inline `generate<X>(dayIndexOf(day),day)` call both use a string; F6 chose string for self-consistency). G1/G3/G4/G5 all matched it.
- **Lint baseline (F0):** `eslint ^8.57.0` (v8 for legacy `.eslintrc.json`, NOT v9/flat-config) + `eslint-config-next ^15.5.19`; `.eslintrc.json` = `{ "extends": "next/core-web-vitals" }`; zero rule suppressions. Steady state = **0 errors / 7 warnings** (5× no-img-element, 2× exhaustive-deps), lint exits 0. Don't reintroduce the removed `eslint-disable no-explicit-any` comments or a post-early-return hook (rules-of-hooks).
- **Shared-file ownership held via slots, not edits:** `globals.css` is F4's sole domain; `queries.ts` is F6's — games mount into RoomShell `leftRail`/`rightRail` slots and never touch either. All 9 games respected this.
- **Pipeline forge layering order:** G2 (board) → G6 (thread, forges from G2's board clues) → G9 (overture audio) all edit `question_forge.py` + `selftest.py` additively. Deezer excluded upstream at G2, so thread chains never hold Deezer music. Seed bank NOT regenerated by any forge-only change (committed seed still passes all gates; Deezer drop only affects future nightly forge).

## Per-unit decisions

- **F0 lint-foundation** — see lint baseline above. 4 errors fixed at source (GoogleMap ×2, ClockGame unescaped entity, MapGame rules-of-hooks). Edited G3/G5 files (they branch after F0, inherit the fixes).
- **F1 design-system** — category color is two-tier: `CATEGORY_HEX` = theme-invariant FILL (SVG/pins/glow); `--cat-*`/`text-{cat}` = text-safe INK (AA both themes). Text via HEX is a bug. Extract codified shipped tokens as-is. G1–G9 load INDEX+FOUNDATIONS+UI-KIT per INDEX §Load map. Drift left open: no shared Button component yet (pattern documented); `lib/games.ts` still lists cold-case.
- **F2 tutorials** — `Tutorial = { href, title, accent:Category, tagline, rules[2-5], example:{prompt,walkthrough} }`. Registry: each `lib/tutorials/<room>.ts` exports `tutorial`; `index.ts` (STABLE) builds `TUTORIALS: Record<href,Tutorial>` + `tutorialByHref`. Games edit ONLY their own file. RoomShell mounts `<TutorialOverlay>`; SSR-safe (closed on server + first paint, post-mount effect reads `localStorage["parlor:tutorial-seen:"+href]`). Cold-case has no entry (retired).
- **F3 numbering** — single source `lib/games.ts GAMES[].rank`, 1–11: mystery, board, clock, wedges, streak, map, gauntlet, thread, seance, ladder, overture. Overture rank 12→11 for contiguity (joker:true, display ignores it). Stripped stale "room NN — " prefixes from 10 RoomShell labels.
- **F4 mobile-panels** — `<CollapsiblePanel side title accent? defaultOpen? storageKey?>`; SSR renders defaultOpen, localStorage hydrates post-mount; collapse disabled under reduced-motion. RoomShell gains `leftRail`/`rightRail` props → `lg:` 3-col sticky grid (rails ~15rem), mobile 1-col stack; desktop unchanged when unused. Mobile rules: `overflow-x: clip` (NOT hidden — breaks sticky), `max-width:100%`, touch ≥44px. **Primitive built here; consumers live in the game branches (Map/Seance/Ladder/Streak/Mystery)** — a branch-scoped view of F4 alone looks unadopted.
- **F5 coldcase-removal** — delete-only. Left historical `20260626_v3.sql` enum value + Mystery-room "Cold Case" flavor strings (that's the Mystery room, not the removed cold-case room).
- **F6 engine-scaffold** — signature `date:string` (see traps). Minimal placeholder interfaces (date/weekday/flavor/seed/solution); games expand. Migrations dated 20260706. Loaders unused until games wire pages → a silent Serena drop wouldn't fail the build; verify the 4 `get<X>Puzzle` exports exist before building on them.
- **G1 sanctum** — `MysteryPuzzle { date, weekday, caseName, seed, suspects[], locations[], times[], clues:MysteryClue[], solution:{suspect,location,time} indices }`. Retired old social-deduction engine (self-contained, no external importers). Reused MysteryStatusPill.
- **G2 codex** — **§5 scope call over brief's literal "all board Wikipedia-provenanced":** strict wiki-only drops the board below 5 categories (goes dark), so scope = drop Deezer, keep multi-source Jeopardy, music via Wikipedia only. Two-layer theme: ~7 visual SKINS (`themes.ts THEMES ↔ BOARD_THEME_KEYWORDS`) + a large anniversary pool mapping many dates onto those skins. Single `frontend/lib/anniversaries.json` read by both frontend (import) and pipeline.
- **G3 chronos** — permutation-CSP ordering puzzle (gear train, N=4 weekday / 5 weekend). Generator bakes a random assembly, enumerates TRUE constraints, subtracts to a minimal uniquely-solving set (verified `solveChronos` cap=2). Trivia (cast year/dialYear) is flavor on the already-unique solution — `solveChronos` never reads a trivia field. Old `clockLogic.ts`/`clock.test.ts`/`.module.css` left intact but unused.
- **G4 ignite** — glyph-substitution cipher; deduce rune→letter bijection from a minimal unique clue set. Rune sets Unicode-only (Elder Futhark, Anglo-Saxon Futhorc, Ogham, Phoenician); every clue names each rune by transliteration → font-tofu never makes a clue ambiguous (solvability font-independent). Subtraction Method pares to minimal-unique. Reduced-motion: candle + ember all `animation:none`. Kept `lib/streak.ts` unchanged (own test still green).
- **G5 atlas** — constellation logic puzzle. **DELETED `components/GoogleMap.tsx`** (keyless raster hybrid, only MapGame used it) → /map is now a star field, renders no Earth map. Offline data = committed `star-catalog.json` (HYG DB CC BY-SA + IAU/Stellarium stick-figures, public domain) = the CLAUDE.md polygon-equivalent local fallback. **`WorldMap.tsx` + `lib/geo.ts` KEPT — GauntletGame still uses them.**
- **G6 thread** — difficulty = within-category popularity percentile (≤2 = easiest ~40% = well-known; data-driven, not a fixed catalog). Link text names both ends (last char == next answer's first) so a stuck player knows the target.
- **G7 seance-ui** — clue rail uses F4 CollapsiblePanel (side=left, storageKey `parlor:seance-hints`, defaultOpen). `clearRowCol`/`clearBoard` are board-STATE helpers over `emptyBoard`/`histCommit` (undoable, no engine call). **Engine UNCHANGED** — no edit to `lib/seance.ts`/`getSeancePuzzle`/clue gen. Triple-click fires clicks 1-2 (cycle) before clearing on 3 — intended, undoable.
- **G8 climb** — extended tagged union `kind: skyscrapers|futoshiki|binairo`. Skyscrapers/futoshiki capped at size 6 (generation-speed budget ~3.5s/day under vitest timeouts); binairo reaches 8 → 5–8 band spans the library, not per-family. Heavy gen tests carry 60s timeouts. `ladderFlavor.ts` SIGILS now-unused (harmless).
- **G9 overture** — Deezer previews exist only when Deezer reachable at forge time; offline falls back to synth-melody rounds (stays zero-env playable). Preview facts carry accurate release-year (keeps THE CLOCK correct). Title rides in `subject_a` (a Deezer URL isn't sluggable). Offline choice richness from `DECOY_TITLES` (previews never reach the committed seed bank).

## Known follow-ups (non-blocking, flagged at land)

- F6 `chronosPuzzle.test.ts:57` — dead-code type error (`const sat = generateChronos(1, day(1).dayIndex)` passes a number where `date:string` expected; `void sat` on the next line, never used). `tsc --noEmit`-only, not caught by the DoD gate (build+test+lint all green). Fix = delete the two lines.
- F2 — no dedicated TutorialOverlay unit test (covered by full suite + manual).
- Design North Star cadence: `design/INDEX.md` is `challenge: each release`, last-challenged 2026-07-06 (created this cycle) — flagged overdue at land; fix = a `~/.claude/design-system-creator/` challenge run. Arguably skip one cycle since brand-new.

---
cost: wave-1 actuals — F1 93k, F5 53k subagent-tok (vs est 150k/80k) ≈ 0.6× heuristic; block peaked ~13.5M/$19, weekly <80%. Units ran under estimate → 7-wide wave-4 affordable.
process: builders frequently maxTurns just before commit/note (resumable stop, not failure); F0 added mid-run (user chose "make lint real" after `npm run lint` found non-functional repo-wide). Blind-judge D-review: ~50% of judges stalled before emitting the verdict JSON when prompts invited deep investigation — batch one "emit verdict now" nudge. Never run judges' builds concurrently in the shared checkout (races corrupt `.next`).
