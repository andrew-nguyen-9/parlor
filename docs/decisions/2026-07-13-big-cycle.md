# 2026-07-13 — Big Cycle (harvested from `.orchestrator/`)

Landed 2026-07-12 spec, 30 units. Distilled from `*.done.md` `decided:`/`gotchas:` before the run dir was reset. App stayed zero-env (seed-bank fallback); all blocker boxes ticked.

## Game codenames & true mechanics (CLAUDE.md labels were wrong)
- **Chronos** (was "Clock/WhenTaken") — watch-assembly sim: mainspring/gear-train/escapement/balance-wheel, wheel-tray tap-to-seat, Mon→Sun difficulty, PBR/Three.js render, WebGL→2D degrade. NOT a photo-year guess.
- **Ignite** (was "Streak/higher-lower") — rune substitution cipher: glyph→letter bijection, clue types (anchor/vowel/consonant/before/adjacent/not), misreads meter, Phaser board + FlameAtmosphere. NOT higher-lower.
- **Atlas** (was "Map/GeoGuessr") — constellation-LOGIC deduction: real star-and-line figures render as inline SVG from committed `star-catalog.json`, ZERO WebGL / ZERO network; 3D ThreeStage is progressive enhancement. `MapGame.tsx` + `lib/atlasPuzzle.ts` are source of truth, not the retired GeoGuessr loop.
- **Mystery** — three-axis board (Suspects/Scene/Hour), potential→prime→cleared pill cycle, Case File evidence layers, single-accusation close.
- **Wedges / Thread / Seance** — logic FROZEN this cycle; work was presentation/skin only.

## Architecture decisions
- **Skin layer**: per-game skin (`theme.ts` tokens + `app/skins.css` `[data-skin=…]`) sits OVER the shared locked floors (f-design E0). Games opt into skins; floors stay locked. Confirm `theme.ts` export surface before engines consume it.
- **Offline invariant honored**: Atlas deduction fully solvable in pure DOM; `WorldMap.tsx` left untouched (GauntletGame's consumer, prop API intact). The env-gated wikimedia raster basemap is the ONE sanctioned CSP/offline exception, lives only in `WorldMap.tsx`.
- **CSP**: no CSP header exists today (only `next.config` image `remotePatterns`, 5 hosts). Cinzel self-hosted via `next/font` (no runtime font CDN). Per-game img-src/degrade matrices, global header shape.

## Deferred (out-of-ownership, follow-up units)
- **Atlas generator** (`lib/atlasPuzzle.ts`): 8-candidate count (still 6), new clue kinds (symmetry/longest-line/nearest-center), localStorage persistence+streak, archive collection, title-card intro, omens-as-floating-glass layout.
- **Ignite**: "identify featured compound" + full chemistry verbs need `IgnitePuzzle` payload work (out-of-ownership lib); candles currently a misread-life meter, not fire-propagation.
- **Seance**: `skins.css` seance block still empty scaffold — a future unit can wire `applySkin("seance")` for the Victorian material palette (v4 `08_seance.txt`).

## Notes
- Intake questionnaires grounded in BOTH the grander `docs/v4/*.txt` vision AND the shipped prototypes; v4 specs are visual/redesign bibles, generally grander than what shipped.
- `design/PATTERNS.md` §Motion (≤1-loop budget) + `design/INDEX.md` §Floors are the live motion authorities.

cost/process: 30 units, worktree isolation recurring gotcha (fresh worktrees need own `npm install` — package-lock ⇒ npm); several dispatch one-liners contradicted the intake/brief (built to intake, not the one-liner).
